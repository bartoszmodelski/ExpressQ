1. Execute following commands in directory containing this file to compile, test and run locally (127.0.0.1:8080): 

	mvn package 
	java -jar target/dependency/webapp-runner.jar target/*.war


2. For Heroku deployment:
2.1 Credentials:
	e-mail: timlucar@norih.com (unaccessible temp. mailbox)
	login:	deltateam
	pass:	DeltaTeam123

2.2 Execute following commands only for the first time, to configurate Heroku:
	heroku
	<you should be asked for e-mail and password, provide them>
	git remote add heroku https://git.heroku.com/desolate-gorge-23142.git
	
2.3 Anytime you want to actually deploy app: 
	git add .
	git commit -m "Comment"
	git push heroku master
	heroku open
	